<div class="user-profile">
  <div class="icon text-center">
    <img src="<%= gravatar_url(@user, size: 100) %>" class="img-circle"></img>
  </div>
  <div class="name text-center">
    <h1><%= @user.name %></h1>
  </div>
</div>